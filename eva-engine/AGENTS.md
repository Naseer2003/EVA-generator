# EVA Engine Development

## Setup

Create and activate virtual environment before running:

```bash
python -m venv venv
.\venv\Scripts\activate  # Windows
source venv/bin/activate     # Unix/Mac
pip install -r requirements.txt
```

The main.py includes a venv check that warns if the virtual environment is missing.